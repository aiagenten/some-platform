[build]
  command = "npm run build"
  publish = ".next"

[functions]
  # Extend function timeout for LLM API calls (text gen + image gen = 15-30s)
  external_node_modules = ["sharp"]

[[plugins]]
  package = "@netlify/plugin-nextjs"

# API routes need longer timeout for AI generation
[functions."api-posts-generate-*"]
  timeout = 60

# Video generation routes need even longer (Kling video = 30-120s, music = 15-30s)
[functions."api-video-*"]
  timeout = 300
